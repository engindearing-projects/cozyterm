{
  "name": "cozyterm",
  "version": "2.0.0",
  "private": true,
  "description": "AI coding agent for the terminal — gets better the more you use it",
  "type": "module",
  "workspaces": [
    "packages/cli",
    "packages/engine",
    "packages/trainer",
    "packages/shared"
  ],
  "scripts": {
    "dev": "bun run --filter '@cozyterm/cli' dev",
    "dev:engine": "bun run --filter '@cozyterm/engine' dev",
    "typecheck": "bun run --filter '*' typecheck",
    "build": "bun run --filter '*' build"
  },
  "license": "MIT",
  "homepage": "https://cozyterm.com",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/engindearing-projects/cozyterm.git"
  }
}
